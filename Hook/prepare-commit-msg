#!/usr/bin/env ruby
$from_branch
$msg

def merge?
	ARGV[1] == "merge"
end

def merge_ws?
  ARGV[2] == "ws"
end

if merge? && merge_ws?
  puts "STOP THE MERGE!"
  puts "Never merge from WS branch! Thanks"
  exit 1
end